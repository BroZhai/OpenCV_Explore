[project]
name = "pyopencv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bro-Tech", email = "k2785233247@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=5.0.0.93",
]

[project.scripts]
pyopencv = "pyopencv:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
